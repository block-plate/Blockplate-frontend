import styled from "styled-components";
import React from "react";

const StyledPageFIX = styled.div`

`

type FIXProps = {
    children: React.ReactNode;
};


const PageFIX = (props: FIXProps) => {
    return (
        <StyledPageFIX>

        </StyledPageFIX>
    )
}

export default PageFIX;
